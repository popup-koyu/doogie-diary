# DOOGIE - Design System (디자인 시스템)

> DOS 레트로 감성의 일관된 UI를 유지하기 위한 디자인 토큰, 컴포넌트, 패턴 정의

**버전**: 1.0
**최종 수정일**: 2026-02-18
**프로젝트**: DOOGIE Diary
**플랫폼**: Web (Vanilla CSS, Single HTML File)

---

## 1. 디자인 토큰 (Design Tokens)

### 1.1 색상 팔레트

#### 핵심 색상

| 토큰 | HEX | 용도 | 사용 빈도 |
|------|-----|------|----------|
| `--dos-black` | `#000000` | 전체 배경, 기본 배경 | ★★★ |
| `--dos-green` | `#00ff00` | 기본 텍스트, 테두리, 아이콘 | ★★★ |
| `--dos-white` | `#ffffff` | 호버 텍스트, 강조 | ★★ |

#### 녹색 계열 (Green Scale)

| 토큰 | HEX | 용도 |
|------|-----|------|
| `--green-900` | `#001a00` | 가장 어두운 배경 (드롭다운 내부) |
| `--green-800` | `#003300` | 비활성 텍스트, 전/후월 날짜, 호버 배경 |
| `--green-700` | `#004400` | 구분선, 비활성 테두리 |
| `--green-600` | `#008800` | 헤더 배경, 로고 그림자, 달력 당월 날짜 |
| `--green-500` | `#00aa00` | 보조 테두리, 마크다운 구분선 |
| `--green-400` | `#00cc00` | 보조 텍스트, 삭선 효과 |
| `--green-300` | `#00ff00` | 기본 (Primary) |
| `--green-200` | `#55ff55` | 밝은 텍스트, 활성 상태 |

#### 특수 색상

| 토큰 | HEX | 용도 |
|------|-----|------|
| `--accent-orange` | `#ff8800` | 마크다운 코드 블록 |
| `--glitch-magenta` | `#ff00ff` | 글리치 효과 (텍스트 그림자) |
| `--glitch-cyan` | `#00ffff` | 글리치 효과 (텍스트 그림자) |
| `--xmas-red` | `#ff0000` | 크리스마스 모드 |
| `--xmas-gold` | `#ffd700` | 크리스마스 모드 |

#### 픽셀 아트 팔레트 (이미지 처리용)

| 단계 | 밝기 | RGB | 미리보기 |
|------|------|-----|---------|
| 0 | 0~42 | `rgb(0, 0, 0)` | ■ 검정 |
| 1 | 43~84 | `rgb(0, 51, 0)` | ■ 매우 어두운 녹색 |
| 2 | 85~126 | `rgb(0, 102, 0)` | ■ 어두운 녹색 |
| 3 | 127~169 | `rgb(0, 153, 0)` | ■ 중간 녹색 |
| 4 | 170~212 | `rgb(0, 204, 0)` | ■ 밝은 녹색 |
| 5 | 213~255 | `rgb(0, 255, 0)` | ■ 최대 녹색 |

---

### 1.2 타이포그래피

#### 폰트 패밀리

| 토큰 | 폰트 | 로드 방식 | 용도 |
|------|------|----------|------|
| `--font-korean` | NeoDunggeunmo | CDN (@font-face) | 한글 본문, 메뉴, 버튼 |
| `--font-english` | VT323 | Google Fonts | 영문, 숫자, 날짜 |
| `--font-title` | Press Start 2P | Google Fonts | DOOGIE 로고 |
| `--font-fallback` | monospace | 시스템 | 폴백 |

#### 폰트 크기

| 토큰 | 크기 | 용도 |
|------|------|------|
| `--text-xs` | 10px | 로고 부제 (Press Start 2P) |
| `--text-sm` | 14px | 마크다운 h6 |
| `--text-base` | 16px | 드롭다운 항목, 마크다운 h5 |
| `--text-md` | 18px | 마크다운 에디터, 인증 입력 |
| `--text-lg` | 20px | 기본 본문 (body), 한글 메뉴 |
| `--text-xl` | 24px | 메뉴 항목, 마크다운 h2 |
| `--text-2xl` | 28px | 마크다운 h1 |
| `--text-3xl` | 36px | 날짜 헤더 (VT323) |
| `--text-4xl` | 48px | 크레딧 타이틀 |
| `--text-hero` | 82px | 메인 로고 "DOOGIE" |

#### 행간 (Line Height)

| 용도 | 값 |
|------|-----|
| 기본 본문 | 1.4 |
| 에디터 | 1.6 |
| 마크다운 프리뷰 | 1.6 |

---

### 1.3 간격 (Spacing)

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--space-xs` | 5px | 버튼 패딩 (세로), 인라인 간격 |
| `--space-sm` | 10px | 메뉴바 패딩, 버튼 패딩 (가로) |
| `--space-md` | 20px | 화면 패딩, 메뉴 항목 gap, 메뉴 간격 |
| `--space-lg` | 30px | 섹션 간 마진 |
| `--space-xl` | 60px | 로고-메뉴 간격 |

---

### 1.4 테두리 (Border)

| 패턴 | 값 | 용도 |
|------|-----|------|
| 기본 테두리 | `1px solid #00ff00` | 컨테이너, 구분선 |
| 강조 테두리 | `2px solid #00ff00` | 오늘 날짜, 선택된 메뉴, 인증 폼 |
| 굵은 테두리 | `3px solid #00ff00` | 이미지 모달 |
| 보조 테두리 | `1px solid #00aa00` | 마크다운 요소 |
| 비활성 테두리 | `1px solid #004400` | 비활성 구분선 |
| 투명 테두리 | `2px solid transparent` | 메뉴 항목 기본 (호버 시 변경) |

---

### 1.5 그림자 (Shadow)

| 패턴 | 값 | 용도 |
|------|-----|------|
| DOS 이중 테두리 | `0 0 0 1px #00ff00, 0 0 0 2px #000000, 0 0 0 3px #00ff00` | 메인 컨테이너 |
| 글로우 | `0 0 5px #00ff00` | 활성 토글, 호버 버튼 |
| 글로우 (밝은) | `0 0 5px #55ff55` | 활성 상태 강조 |
| 별 글로우 | `0 0 2px #00ff00` | 배경 별 |
| 텍스트 그림자 | `6px 6px 0px #008800` | 로고 |

---

### 1.6 z-index 계층

> **참고**: CSS 변수로 정의되지 않고 직접 숫자값으로 사용됩니다.

| 값 | 용도 |
|-----|------|
| 0~1 | 기본 콘텐츠, 별, 메뉴 |
| 10 | 날짜 헤더 |
| 100 | 메뉴바 (sticky) |
| 1000 | 드롭다운 메뉴 |
| 1001 | 드롭다운 오버레이 |
| 2000 | 크롭 모달 |
| 3000 | 크롭 오버레이 |
| 9998 | Picture 드롭다운 (fixed) |
| 9999 | List 드롭다운 (fixed) |
| 10000 | 확인 다이얼로그 |
| 10001 | 다이얼로그 배경 |

---

## 2. 핵심 컴포넌트 (Core Components)

### 2.1 버튼 (Button)

#### 변형 (Variants)

| 변형 | 기본 상태 | 호버 상태 | 용도 |
|------|----------|----------|------|
| **Default** | 투명 배경 + 녹색 텍스트 | 흰색 텍스트 | 메뉴바 항목 |
| **Primary** | 투명 배경 + 녹색 테두리 | 녹색 배경 + 검정 텍스트 | 타이틀 메뉴 |
| **Active** | 녹색 배경 + 검정 텍스트 | - | 선택된 탭 |
| **Toggle ON** | `#003300` 배경 + 녹색 테두리 | - | 설정 토글 |
| **Toggle OFF** | 투명 배경 | - | 설정 토글 |

#### CSS 패턴

```css
/* 메뉴바 버튼 (Default) */
.menu-item-top {
    cursor: pointer;
    padding: 5px 10px;
    color: #00ff00;
    transition: color 0.2s;
}
.menu-item-top:hover {
    color: #ffffff;
}

/* 타이틀 메뉴 (Primary) */
.menu-item {
    font-family: 'NeoDunggeunmo', monospace;
    font-size: 24px;
    color: #00ff00;
    padding: 6px 20px;
    border: 2px solid transparent;
    transition: all 0.2s;
}
.menu-item:hover, .menu-item.selected {
    background: #00ff00;
    color: #000000;
    border: 2px solid #00ff00;
}
```

---

### 2.2 입력 (Input)

| 변형 | 용도 | 특징 |
|------|------|------|
| **Textarea** | 일기 에디터 | 전체 높이, 투명 배경, 녹색 텍스트 |
| **Text Input** | 인증 폼 | 녹색 테두리, 어두운 배경 |
| **PIN Input** | PIN 입력 | 마스킹, 4자리 제한, inputmode=numeric |

```css
/* 인증 입력 */
.auth-input {
    background: #001a00;
    border: 1px solid #00aa00;
    color: #00ff00;
    padding: 10px;
    font-family: 'NeoDunggeunmo', monospace;
    font-size: 18px;
}
.auth-input:focus {
    border-color: #00ff00;
    outline: none;
    box-shadow: 0 0 5px rgba(0, 255, 0, 0.3);
}

/* 에디터 */
.editor {
    background: transparent;
    color: #00ff00;
    border: none;
    font-family: 'NeoDunggeunmo', monospace;
    font-size: 20px;
    line-height: 1.6;
    resize: none;
}
```

---

### 2.3 카드/컨테이너 (Container)

| 변형 | 용도 | 테두리 |
|------|------|--------|
| **Main Container** | 메인 화면 프레임 | DOS 이중 테두리 (box-shadow) |
| **Panel** | 드롭다운, 설정 섹션 | 1px solid #00ff00 |
| **Auth Container** | 인증 폼 | 2px solid #00ff00 |

```css
/* DOS 이중 테두리 컨테이너 */
.main-container {
    border: 1px solid #00ff00;
    background: #000000;
    box-shadow:
        0 0 0 1px #00ff00,
        0 0 0 2px #000000,
        0 0 0 3px #00ff00;
}
```

---

### 2.4 드롭다운 (Dropdown)

```css
/* 드롭다운 패널 */
.dropdown-panel {
    background: #001a00;
    border: 1px solid #00ff00;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
}

/* 드롭다운 항목 */
.dropdown-item {
    padding: 8px 12px;
    color: #00ff00;
    border-bottom: 1px solid #004400;
    cursor: pointer;
}
.dropdown-item:hover {
    background: #003300;
    color: #ffffff;
}
```

**동작 규칙**:
- 하나가 열리면 다른 것 자동 닫힘
- 외부 클릭 시 닫힘 (100ms 딜레이)
- max-height transition으로 슬라이드 애니메이션

---

### 2.5 모달/다이얼로그 (Modal)

| 변형 | 용도 | 특수 효과 |
|------|------|----------|
| **Image Modal** | 이미지 전체 화면 | 스캔 효과 (1.5초) |
| **Confirm Dialog** | 저장 확인 | DOS 이중 테두리 + 드롭섀도우 |
| **Crop Modal** | 이미지 크롭 | 드래그 + 리사이즈 핸들 |

```css
/* 모달 오버레이 */
.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    z-index: 5000;
}

/* 확인 다이얼로그 */
.confirm-dialog {
    position: fixed;
    border: 3px double #00ff00;
    background: #000000;
    box-shadow: 5px 5px 0 #003300;
    z-index: 10000;
    padding: 20px;
}
```

---

### 2.6 탭 (Tabs)

```css
/* 인증 화면 탭 */
.auth-tab {
    background: transparent;
    color: #008800;
    border: 1px solid #004400;
    padding: 8px 20px;
    cursor: pointer;
}
.auth-tab.active {
    background: #003300;
    color: #00ff00;
    border-color: #00ff00;
}
```

---

### 2.7 달력 (Calendar)

```css
/* 달력 그리드 */
.calendar-grid {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
}

/* 날짜 셀 */
.calendar-day {
    color: #008800;          /* 당월 */
    padding: 8px;
    cursor: pointer;
}
.calendar-day.other-month {
    color: #003300;          /* 전/후월 */
}
.calendar-day.today {
    border: 2px solid #00ff00;  /* 오늘 */
}
.calendar-day.has-entry::after {
    content: '';
    width: 4px;
    height: 4px;
    background: #00ff00;
    border-radius: 50%;       /* 녹색 점 */
}
.calendar-day:hover {
    background: #003300;
}
```

---

## 3. 복합 컴포넌트 (Composite Components)

### 3.1 메뉴바 (Menu Bar)

```
┌──────────────────────────────────────────────────────────────┐
│ [Home] [New] [Save] [Picture▾] [List▾] [Help] [🎄] [Full]   │
│ ──────────────────────────── [0/100] [DIARY ○] [닉네임] [Logout] │
└──────────────────────────────────────────────────────────────┘
```

- `display: flex`, `gap: 20px`, `align-items: center`
- `position: sticky`, `top: 0`, `z-index: 100`
- `border-bottom: 1px solid #00ff00`
- 반응형: 768px 이하에서 일부 숨김, 480px 이하에서 5개만 표시

### 3.2 에디터 영역 (Editor Area)

```
┌──────────────────┬──────────────────┐
│  날짜 헤더        │                  │
├──────────────────┤                  │
│                  │  이미지 영역      │
│  텍스트 에디터    │  (Grid)          │
│                  │                  │
└──────────────────┴──────────────────┘
```

- `display: grid`, `grid-template-columns: 1fr 1fr` (이미지 있을 때)
- 이미지 없으면 텍스트 100%
- 모바일: `flex-direction: column`, 이미지(order:1) 위, 텍스트(order:2) 아래

### 3.3 인증 폼 (Auth Form)

```
┌─────────────────────────┐
│  [로그인] [회원가입]      │  ← 탭
│                         │
│  닉네임: [________] ✓   │  ← 실시간 중복 체크
│  PIN:    [****]         │  ← 마스킹
│  [     로그인     ]      │  ← 제출 버튼
│                         │
│  ← 돌아가기              │
└─────────────────────────┘
```

---

## 4. 애니메이션 시스템

### 4.1 트랜지션

| 속성 | 기본값 | 용도 |
|------|--------|------|
| `transition: color` | `0.2s` | 메뉴 호버 |
| `transition: all` | `0.2s` | 버튼 호버 |
| `transition: max-height` | `0.3s ease` | 드롭다운 |
| `transition: opacity` | `0.3s` | 페이드 인/아웃 |

### 4.2 키프레임 애니메이션

#### 상시 실행

| 이름 | 대상 | 시간 | 효과 |
|------|------|------|------|
| `noise` | `body::before` | 0.5s steps(8) infinite | CRT 노이즈 |
| `twinkle` | `.star` | 3s infinite | 별 깜빡임 |
| `glitch` | `.logo` | 3s infinite | 로고 글리치 |
| `cursorBlink` | `.cursor` | 0.8s infinite | 커서 깜빡임 |
| `glowPulse` | `.company-text` | 2s infinite | 글로우 펄스 |

#### 트리거 실행

| 이름 | 트리거 | 시간 | 효과 |
|------|--------|------|------|
| `scanReveal` | 이미지 모달 열기 | 1.5s | clip-path 스캔 |
| `scanline` | 이미지 모달 열기 | 1.5s | 스캔라인 바 이동 |
| `driveRight` | 크레딧 화면 | 6-10s | 자동차 좌→우 |
| `driveLeft` | 크레딧 화면 | 6-10s | 자동차 우→좌 |

#### 계절 한정

| 이름 | 조건 | 시간 | 효과 |
|------|------|------|------|
| `snowfall` | 12월 (크리스마스 모드) | 3-8s | 눈 내리기 |
| `twinkleChristmas` | 크리스마스 모드 | 변동 | 크리스마스 별 |
| `treeSparkle` | 크리스마스 모드 | - | 트리 반짝임 |

---

## 5. 반응형 패턴

### 5.1 브레이크포인트

```css
/* Desktop: 기본 스타일 */

/* Tablet: ≤768px */
@media (max-width: 768px) { }

/* Mobile: ≤480px */
@media (max-width: 480px) { }

/* Touch: 터치 기기 */
@media (hover: none) and (pointer: coarse) { }
```

### 5.2 컴포넌트별 반응형 변화

| 컴포넌트 | Desktop | Tablet | Mobile |
|---------|---------|--------|--------|
| 로고 | 82px | 82px | 48px, letter-spacing: 6px |
| 메뉴바 | 전체 | 축소 | 5개까지 |
| 에디터 | 좌우 1:1 | 좌우 1:1 | 세로 (이미지↑ 텍스트↓) |
| 이미지 갤러리 | Grid 2xN | Grid | 가로 스와이프 180x170 |
| 달력 | 좌우 분할 | 좌우 분할 | 세로 스택 |
| 모드 토글 | 표시 | 숨김 | 숨김 |
| 사용자 정보 | 표시 | 축소 | 숨김 |
| 터치 영역 | - | - | 최소 44px |

---

## 6. Do's and Don'ts

### Do's (이렇게 하세요)

| 규칙 | 예시 |
|------|------|
| 녹색 팔레트 내에서만 색상 선택 | `#00ff00`, `#008800`, `#003300` |
| 모든 텍스트에 monospace 폰트 사용 | `'NeoDunggeunmo'`, `'VT323'` |
| 호버 시 색상 반전 또는 밝게 | 배경 녹색 + 텍스트 검정 |
| DOS 스타일 테두리 사용 | `1px solid #00ff00` |
| 애니메이션은 transform/opacity 위주 | GPU 가속, 레이아웃 변경 최소화 |
| 새 컴포넌트는 기존 패턴 참조 | 이 문서의 CSS 패턴 복사 |

### Don'ts (이렇게 하지 마세요)

| 금지 | 이유 |
|------|------|
| 녹색 계열 외 색상 사용 | DOS 테마 깨짐 (예외: 마크다운 코드 #ff8800) |
| 둥근 모서리 (border-radius) | DOS는 각진 디자인 (현재 예외: 달력 점 `50%`, 마크다운 코드 블록 `3px`/`5px`, 모드 토글 `12px`) |
| 그라데이션 배경 | DOS 감성에 맞지 않음 (예외: 크리스마스 모드) |
| Sans-serif 폰트 | 반드시 monospace 계열 (현재 예외: 마크다운 프리뷰에서 `-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif` 사용) |
| 부드러운 그림자 (box-shadow blur) | DOS는 하드 엣지 (0px blur) |
| opacity로 비활성 처리 | 색상을 어둡게 (#003300)으로 처리 |
